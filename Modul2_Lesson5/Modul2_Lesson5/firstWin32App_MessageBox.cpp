#include "stdafx.h"

//int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, PSTR cmdLine, int showCmd)
//{
    // L-prefix before string means it's a unicode string, e.g. L"First Win32 Program"
    // only used when unicode is set in project properties
    //int r = MessageBox(0, "First Win32 Program", "Window Title", MB_OKCANCEL);

    //if(r == IDOK)
    //{
    //    MessageBox(0, "OK was pressed", "", MB_OK);
    //} 
    //else if (r == IDCANCEL)
    //{
    //    MessageBox(0, "Cancel was pressed", "", MB_OK);
    //}
//}
